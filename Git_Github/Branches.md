# Branches

## What is a Branch?

A branch allows development without affecting main.

## Common Workflow

main
│
├── feature-login
│
├── feature-api
│
└── feature-ui

## Common Commands

Create branch:

git checkout -b branch_name

Switch branch:

git checkout branch_name

List branches:

git branch

Merge branch:

git merge branch_name